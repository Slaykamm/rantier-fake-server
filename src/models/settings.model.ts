export interface ISetSettingsDto {
  email?: boolean;
  telegramm?: boolean;
  counter_reminder?: boolean;
  payment_reminder?: boolean;
  notification_service?: boolean;
  auto_invoicing?: boolean;
}
